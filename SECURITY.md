# Security policy

Report vulnerabilities privately to security@djconnect.dev. Never report or
publish Broadcast tokens, pairing tokens, Home Assistant URLs, diagnostics or
user/session data. The receiver must retain the handoff only in memory.
