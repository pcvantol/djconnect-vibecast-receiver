# Contributing

Run `npm test`, keep changes small, and use pull requests. Do not add secrets,
analytics, persistence, a proxy, receiver controls, Home Assistant credentials
or user data. Cross-repository contract changes require a coordinated update in
`pcvantol/djconnect`.
